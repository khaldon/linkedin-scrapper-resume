# ============================================================================
# LinkedIn Scraper Resume - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# Google Gemini API Key for CV Generation
# Get your FREE API key from: https://makersuite.google.com/app/apikey
# This is REQUIRED for CV generation to work
GOOGLE_API_KEY=your_google_api_key_here

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# Option 1: Supabase PostgreSQL (RECOMMENDED for Production/Hugging Face)
# ----------------------------------------------------------------------------
# Why Supabase?
# - Free tier available (500 MB database, 5 GB bandwidth)
# - Persistent storage (perfect for Hugging Face Spaces)
# - Production-grade PostgreSQL
# - Easy setup and management
#
# Setup Instructions:
# 1. Create account at https://supabase.com (free)
# 2. Create a new project
# 3. Go to Settings -> Database -> Connection String
# 4. Copy the URI and replace [YOUR-PASSWORD] with your actual password
#
# Format: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
# Example: postgresql://postgres:mySecurePass123@db.xyzproject.supabase.co:5432/postgres
SUPABASE_DATABASE_URL=postgresql://postgres:password@db.project.supabase.co:5432/postgres

# Option 2: Alternative PostgreSQL Connection
# ----------------------------------------------------------------------------
# If you're using a different PostgreSQL provider, use this instead
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Option 3: Local SQLite (Default for Development)
# ----------------------------------------------------------------------------
# Leave SUPABASE_DATABASE_URL and DATABASE_URL empty to use SQLite
# Note: SQLite is NOT recommended for Hugging Face (ephemeral storage!)
DATABASE_PATH=data/jobs.db

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# LinkedIn Credentials (Optional - for authenticated scraping)
# ----------------------------------------------------------------------------
# The app works WITHOUT LinkedIn credentials (anonymous scraping)
# Only needed if you want to scrape private/detailed job information
LINKEDIN_EMAIL=your_email@example.com
LINKEDIN_PASSWORD=your_password_here

# Browser Settings
# ----------------------------------------------------------------------------
# HEADLESS=True  -> Run browser in background (recommended for production)
# HEADLESS=False -> Show browser window (useful for debugging)
HEADLESS=True

# Server Configuration
# ----------------------------------------------------------------------------
# Port for the web server (default: 7860 for Hugging Face compatibility)
PORT=7860

# ============================================================================
# HUGGING FACE SPACES DEPLOYMENT
# ============================================================================
# When deploying to Hugging Face Spaces:
#
# 1. DO NOT copy .env to your repository
# 2. Instead, add these as "Repository Secrets" in your Space settings:
#    - SUPABASE_DATABASE_URL (required)
#    - GOOGLE_API_KEY (required)
#    - HEADLESS=True (optional, defaults to True)
#
# 3. The app will automatically detect it's running on HF Spaces
#
# See docs/HUGGINGFACE_DEPLOYMENT.md for detailed instructions

# ============================================================================
# VERIFICATION
# ============================================================================
# After configuration, run the verification script:
#   uv run python verify_setup.py
#
# This will check:
# ✅ All dependencies installed
# ✅ Environment variables set correctly
# ✅ Database connection working
# ✅ Ready for deployment

# ============================================================================
# SECURITY REMINDERS
# ============================================================================
# ⚠️  NEVER commit .env to Git
# ⚠️  NEVER share your API keys publicly
# ⚠️  Use different credentials for development and production
# ⚠️  Rotate your passwords regularly
# ✅  Use Hugging Face Secrets for production deployment
# ✅  Use Supabase for persistent storage on Hugging Face
